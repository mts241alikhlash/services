# API client staging

The nine package directories reserve names for generated clients. Generated
source is intentionally absent until each OpenAPI baseline is committed and the
generator versions are pinned.
